# GET /Conditions
get:
  description: Returns all world conditions (global traits)
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../openapi.yml#/components/schemas/Trait'